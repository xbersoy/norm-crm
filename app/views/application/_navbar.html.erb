<nav class="navbar navbar-expand-lg navbar-dark bg-primary fixed-top py-4 blur">
  <div class="container-xl">
    <%= link_to "NormCRM", root_path, class: "navbar-brand" %>
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbar-header" aria-controls="navbar-header" aria-expanded="false">
      <i class="navbar-toggler-icon"></i>
    </button>
    <div class="collapse navbar-collapse" id="navbar-header">
      <ul class="navbar-nav mx-lg-auto">
        <%= content_tag(:li, link_to("Home", "#", class: "nav-link"), class:'nav-item px-3') %>
        <%= content_tag(:li, link_to("Features", '#features', class: "nav-link"), class:'nav-item px-3') %>
        <%= content_tag(:li, link_to("Pricing", "#pricing", class: "nav-link"), class:'nav-item px-3') %>
        <%= content_tag(:li, link_to("Blog", root_path, class: "nav-link"), class:'nav-item px-3') %>
      </ul>
      <div class="navbar-nav align-items-lg-center">
        <% if user_signed_in? %>
          <%= button_to("Logout", destroy_user_session_path, method: :delete, data: { confirm: 'Are you sure?' }, class: "btn btn-light rounded-pill px-3 mx-3") %>
        <%else%>
          <%= link_to("Sign In", new_user_session_path, class: "btn btn-light rounded-pill px-3 mx-3") %>
          <%= link_to("Sign Up", new_user_registration_path, class: "btn btn-primary rounded-pill px-3 mx-3 border-white") %>
        <%end%>
      </div>
    </div>
  </div>
</nav>
